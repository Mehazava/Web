@using Show.Models
@model IEnumerable<LazySong>
    <table class="table">
        <tr>
            <th>Name</th>
            <th>Rating</th>
            <th>Producer</th>
        </tr>
        @foreach (LazySong s in Model)
        {
            <tr class="table">
                <td>@s.Name</td>
                <td>@s.Rating</td>
                <td>@s.Producer.Name</td>
            </tr>
        }
    </table>
